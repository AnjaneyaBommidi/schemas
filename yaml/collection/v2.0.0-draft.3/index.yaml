id: http://schema.getpostman.com/json/collection/v2.0.0-draft.3/
title: Postman Collection Format v2.0
type: object
required:
  - postman
  - info
properties:
  postman:
    type: number
    minimum: 2.0
    maximum: 2.0
    description: The Postman Collection Format version
  info:
    $ref: "#/definitions/Info"
  variables:
    $ref: "#/definitions/Variables"
#  schema:
#    $ref: "#/definitions/Schema"
  requests:
    $ref: "#/definitions/Items"
  examples:
    $ref: "#/definitions/Items"
  tests:
    $ref: "#/definitions/Items"
  scripts:
    $ref: "#/definitions/Scripting/definitions/Scripts"

patternProperties:
  "^_":
    $ref: types.yaml#/definitions/AnyType
additionalProperties: false

definitions:
  Info:
    $ref: info.yaml
  Version:
    $ref: version.yaml
  Variables:
    $ref: variables.yaml
#  Schema:
#    $ref: schema/schema.yaml
  Folder:
    $ref: folders/folder.yaml
  Items:
    $ref: folders/items.yaml
  Items:
    $ref: folders/items.yaml
  Request:
    $ref: requests/request.yaml
  Response:
    $ref: requests/response.yaml
  Cookies:
    $ref: requests/cookies.yaml
  Scripting:
    $ref: requests/scripting.yaml
  Types:
    $ref: types.yaml
